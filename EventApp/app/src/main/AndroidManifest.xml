<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.event.app.izhar.eventappbeta">

    <!--LEAVE THESE COMMENTED !-->
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!-- <uses-permission android:name="android.permission.GET_ACCOUNTS" /> -->
    <!-- <uses-permission android:name="android.permission.READ_PROFILE" /> -->
    <!-- <uses-permission android:name="android.permission.READ_CONTACTS" /> -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" android:required="false" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera2" android:required="false"/>

<!--TODO clean up activities that are not needed ?-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.event.app.izhar.eventappbeta.Initializer">
        <activity android:name="com.event.app.izhar.eventappbeta.LoginActivity"/>
        <activity android:name="com.event.app.izhar.eventappbeta.LoginTwoActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent-filter>
        </activity>
        <activity android:name="com.event.app.izhar.eventappbeta.RegisterUserActivity" />
        <activity android:name="com.event.app.izhar.eventappbeta.Camera" />
        <activity android:name="com.event.app.izhar.eventappbeta.AdminMemberOperations" />
        <activity android:name="com.event.app.izhar.eventappbeta.EventDetailsNavigationDrawer" />
        <activity android:name="com.event.app.izhar.eventappbeta.AccountDetailsNavigationDrawer" />
        <activity
            android:name="com.event.app.izhar.eventappbeta.EventNavigationDrawer"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
      </activity>
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.event.app.izhar.eventappbeta.fileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>
    </application>
</manifest>